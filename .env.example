# ── API keys for remote LLM providers ──────────────────────────────
# Copy this file to .env and fill in the keys you need.
# Only the provider set in config.txt requires a key.
#
#   cp .env.example .env
#
# .env is git-ignored and will never be committed.
# ───────────────────────────────────────────────────────────────────

# OpenAI  (https://platform.openai.com/api-keys)
# OPENAI_API_KEY=sk-...

# Anthropic  (https://console.anthropic.com/settings/keys)
# ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini  (https://aistudio.google.com/apikey)
# GOOGLE_API_KEY=AI...

# Remote Ollama  (optional — point to a more powerful machine)
# OLLAMA_HOST=http://192.168.1.100:11434

# ── LangSmith tracing (optional) ──────────────────────────────────
# Uncomment both lines to enable automatic tracing of every
# LangChain call (no code changes needed).
# Sign up free: https://smith.langchain.com/
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=ls-...

